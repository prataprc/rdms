[package]
name = "rdms"
version = "0.0.1"
authors = ["prataprc <prataprc@gmail.com>"]
description = "Composable storage for key,value index"
documentation = "https://docs.rs/rdms"
homepage = "https://github.com/bnclabs/rdms"
repository = "https://github.com/bnclabs/rdms"
keywords = ["storage", "mvcc", "llrb", "btree", "lsm"]
categories = ["data-structures", "database"]
license = "AGPL-3.0"
exclude = ["template/**", "docs/**"]
readme = "README.md"
edition = "2018"

[lib]
name = "rdms"
path = "src/lib.rs"

[[bin]]
name = "rdms-perf"
required-features = ["rdms-perf"]

[badges]
maintenance = { status = "actively-developed" }
travis-ci = { repository = "bnclabs/rdms" }

[dependencies]
cbordata = { path = "../cbordata", version = "0.5.0" }
arbitrary = { version = "1.0.2", features = ["derive"] }
toml = "0.5.3"
lazy_static = "1.2.0"
fs2 = "0.4.3"
xorfilter-rs = { version = "0.5.1", path = "../xorfilter" }

structopt = { version = "0.3.20", default-features = false, optional = true }
rand = { version = "0.7.3", features = ["small_rng"], optional = true }
lmdb = { version = "0.8.0", optional = true }

# jsondata = "0.6.2"
# jsondata = { git = "https://github.com/bnclabs/jsondata.git" }
# log = "0.4.8"
# memmap = "0.7.0"
# croaring = "0.4.5"
# crc = "^1.0.0"
# sys-info = "0.5.10"

[dev-dependencies]
rand = "0.6.4"

[features]
rdms-perf = ["structopt", "rand", "lmdb"]
